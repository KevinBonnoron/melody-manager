# Go backend (embedded PocketBase) serves BOTH PocketBase and the custom /api
# on a single port — point both URLs at it.
VITE_PB_URL=http://localhost:8090
VITE_SERVER_URL=http://localhost:8090/api
VITE_REGISTRATION_DISABLED=false
