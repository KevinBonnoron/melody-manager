{
  "name": "@melody-manager/server",
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outfile=dist/index.js --target=bun --minify --external=ffmpeg-static --external=music-metadata",
    "build:types": "tsc",
    "dev": "bun --watch run src/index.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.5",
    "@melody-manager/plugin-sdk": "workspace:*",
    "@melody-manager/shared": "workspace:*",
    "bandcamp-fetch": "^3.0.0",
    "eventsource": "^4.1.0",
    "hono": "^4.7.11",
    "lru-cache": "^11.2.4",
    "music-metadata": "^11.10.2",
    "sonos": "^1.14.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
