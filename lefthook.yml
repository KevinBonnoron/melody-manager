pre-commit:
  jobs:
    - name: biome format
      glob: "*.{js,ts,jsx,tsx,json,css}"
      run: bunx biome format --write {staged_files} && git add {staged_files}

pre-push:
  jobs:
    - name: test
      run: bun run test
