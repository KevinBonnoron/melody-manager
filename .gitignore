# .gitignore
# dependencies
node_modules
.pnp
.pnp.js

# testing
coverage

# production
dist
build
cookies/

# core dumps
core.*

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor directories and files
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Bun
bun.lockb
.turbo

# Tanstack
.tanstack

# Pocketbase
api/pb_data

# Nix
.direnv

# Claude
.claude

# VitePress
docs/.vitepress/cache
docs/.vitepress/dist

# Android
android/*.keystore
android/*.jks
android/app/release.keystore
android/gradle.properties
android/local.properties

# iOS
ios/App/App/GoogleService-Info.plist

# Capacitor generated assets (auto-generated by CI)
android/app/src/main/res/mipmap-*
android/app/src/main/res/drawable-*
ios/App/App/Assets.xcassets/AppIcon.appiconset/
ios/App/App/Assets.xcassets/Splash.imageset/

# Operator configuration, generated on first run
api/config/

# Compiled binaries: `task build:api` and `task desktop` both write here.
bin/

# Desktop client: the embedded bundle is a build artefact. Its parent folder is
# kept so `go:embed` resolves on a fresh clone, before anything has been built.
desktop/frontend/dist/

# `go build ./...` inside desktop/ drops the binary beside its source.
desktop/desktop

# Playwright: the throwaway server's database and the run's own output.
e2e/.data/
e2e/.artifacts/
e2e/.report/
